{
  "name": "morse-audio-decoder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "deploy": "npm run build && wrangler pages deploy dist --project-name codigomorse-link --branch main",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@breezystack/lamejs": "^1.2.7",
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
